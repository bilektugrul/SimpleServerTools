join-message:
  enabled: false
  per-group:
    enabled: false
    groups:
      default: |
        default group message
  per-permission:
    enabled: false
    permissions:
      default: | #sst.joinmessages.default
        default permission message
  everyone:
    enabled: true
    message: |
      %prefix% &9Welcome to our server, %player%!